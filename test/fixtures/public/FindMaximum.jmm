import ioPlus;
class FindMaximum {
    int[] test_arr;
    int sou;

    public int find_maximum(int[] arr) {
        int i;
        int maximum;
        int value;
        int[] oi;

        i = 1;
        maximum = i;
        sou = 2 + 3 * this.find_maximum(new int[2]);
        test_arr = new int[i + this.ola()];
        while (i < test_arr.length + 2) {
            value = 0;
            if (maximum < value){
                value = maximum;
            }
            else{

            }
            i = i + 1;
        }

        return maximum;
    }

    public int ola() {
        return 2;
    }
}