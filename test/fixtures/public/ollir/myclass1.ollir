myClass {
	.construct myClass().V{
		invokespecial(this, "<init>").V;
	}
	
	.method public sum(i.i32, j.i32).i32 {
		sum.i32 :=.i32 0.i32;
		sum.i32 :=.i32 2.i32;
		aux.i32 :=.i32 sum.i32;
		sum.i32 :=.i32 aux.i32;
		sum.i32 :=.i32 sum.i32 +.i32 i.i32;
		sum.i32 :=.i32 sum.i32 -.i32 i.i32;
		sum.i32 :=.i32 sum.i32 *.i32 i.i32;
		sum.i32 :=.i32 sum.i32 /.i32 i.i32;
		ret.i32 sum.i32;
	}
}