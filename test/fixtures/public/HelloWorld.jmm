import ioPlus;
class HelloWorld {
	public static void main(String[] a) {
        int[] L;
        int i;
        boolean d;
        Lazysort q;

        L = new int[10];

        i = 0;
        while (i ==) {
            L[i] = L.length - i;
            i = i + 1;
        }

        while (i ==) {
            L[i] = L.length - i;
            i = i + 1;
        }

        while (i ==) {
            L[i] = L.length - i;
            i = i + 1;
        }

        q = new Lazysort();

        q.quicksort(L);
        d = q.printL(L);
    }
}